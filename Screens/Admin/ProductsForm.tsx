import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ProductsForm = () => {
	return (
		<View>
			<Text>Products form</Text>
		</View>
	);
};

export default ProductsForm;

const styles = StyleSheet.create({});
